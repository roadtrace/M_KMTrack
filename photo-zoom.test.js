const test=require('node:test'),assert=require('node:assert/strict');const zoom=require('./photo-zoom.js');
test('photo zoom clamps between one and five times',()=>{assert.equal(zoom.nextScale(1,-1),1);assert.equal(zoom.nextScale(1,1),1.5);assert.equal(zoom.nextScale(5,1),5);});
test('pinch distance and panning bounds are deterministic',()=>{assert.equal(zoom.distance({x:0,y:0},{x:3,y:4}),5);assert.deepEqual(zoom.clampOffset({x:500,y:-500},2,{width:300,height:200},{width:250,height:150}),{x:100,y:-50});assert.deepEqual(zoom.clampOffset({x:30,y:30},1,{width:300,height:200},{width:250,height:150}),{x:0,y:0});});
test('edit thumbnail and photo viewer expose the shared accessible zoom flow',()=>{const fs=require('node:fs'),html=fs.readFileSync(require.resolve('./index.html'),'utf8'),ui=fs.readFileSync(require.resolve('./photo-viewer.js'),'utf8'),sw=fs.readFileSync(require.resolve('./sw.js'),'utf8');assert.match(ui,/viewPhotoForEntry\(editingEntryIndex\)/);assert.match(html,/id="photo-preview"[^>]*role="button"[^>]*tabindex="0"/);for(const id of ['photo-zoom-out','photo-zoom-in','photo-zoom-reset','photo-zoom-close'])assert.match(html,new RegExp(`id="${id}"`));for(const file of ['photo-viewer.css','photo-zoom.js','photo-viewer.js'])assert.match(sw,new RegExp(file.replace('.','\\.')));});
